%div.panel.panel-primary#panel-center
  %div.panel-heading Lista turniejów
  %div.panel-body
    %div.list-group
      - @tournaments.each do |t|
        %a.list-group-item{href: "#"}
          =t.name
          =t.description
          = "(od #{l t.start_date})"
          - if t.tournament_type?
            = "[turniej typu #{t.tournament_type}]"